<form method="post" action="{{ path('ingresos_delete', {'id': ingreso.id}) }}" onsubmit="return confirm('¿Desea eliminar este registro?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ingreso.id) }}">
    <button class="btn btn-danger">Eliminar Ingreso</button>
</form>
