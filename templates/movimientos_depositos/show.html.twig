{% extends 'base.html.twig' %}

{% block title %}MovimientosDepositos{% endblock %}

{% block body %}
    <h1>MovimientosDepositos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ movimientos_deposito.id }}</td>
            </tr>
            <tr>
                <th>FechaMovimiento</th>
                <td>{{ movimientos_deposito.fechaMovimiento ? movimientos_deposito.fechaMovimiento|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Monto</th>
                <td>{{ movimientos_deposito.monto }}</td>
            </tr>
            <tr>
                <th>Banco</th>
                <td>{{ movimientos_deposito.banco }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('movimientos_depositos_index') }}">back to list</a>

    <a href="{{ path('movimientos_depositos_edit', {'id': movimientos_deposito.id}) }}">edit</a>

    {{ include('movimientos_depositos/_delete_form.html.twig') }}
{% endblock %}
